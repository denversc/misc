These are skills for AI agents.

See https://agentskills.io/ and https://geminicli.com/docs/cli/skills/ for details.

To install these skills into gemini-cli, run this command:

```
gemini skills link $PWD
```

where `$PWD` is the absolute path to the directory containing this file.
